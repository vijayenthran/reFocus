import React from 'react';
import ReactDOM from 'react-dom';

const title = 'My Minimal React Webpack Babel Setup';
// minimal React scaffold project
ReactDOM.render(
    <div>{title}</div>,
    document.getElementById('app')
);